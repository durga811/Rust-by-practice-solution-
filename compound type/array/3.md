```rust

fn main() {
    // Fill the blank
    let list: [i32; 100] = [1;100] ;//same element in an array can be achieved by santax [element; no of element]

    assert!(list[0] == 1);
    assert!(list.len() == 100);

    println!("Success!");
}
```
